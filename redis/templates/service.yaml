apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.redis.name }}
  labels:
{{- toYaml .Values.redis.labels | nindent 4 }}
spec:
  ports:
  - port: 3306            # the port on which the service is listening
    protocol: TCP
    targetPort: {{ .Values.redis.containerPorts.mysql }}      # the port exposed by Pod (we defined in the deployment as part of `containerPort)
  selector:
{{- toYaml .Values.redis.labels | nindent 4 }}       # Pod label that service is using to select Pods